# Production Environment Variables for Vercel
# 
# IMPORTANT: Add these in Vercel Dashboard > Project Settings > Environment Variables
# DO NOT commit this file with real values!

# ===== REQUIRED - Get these ready before deployment =====

# MongoDB Atlas Connection String
# 1. Go to MongoDB Atlas (https://cloud.mongodb.com)
# 2. Click "Connect" on your cluster
# 3. Choose "Connect your application"
# 4. Copy the connection string
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/propertypeedika?retryWrites=true&w=majority

# JWT Secret (Generate with: openssl rand -base64 32)
# MUST be at least 32 characters!
JWT_SECRET=

# Cloudinary Credentials
# Get from: https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# ===== PRODUCTION SETTINGS =====

# Environment
NODE_ENV=production

# Allowed Origins (Your Vercel domain)
# After first deployment, update this with your actual Vercel URL
ALLOWED_ORIGINS=https://your-app.vercel.app

# API URL (Use relative path for Vercel)
VITE_API_URL=/api

# Port (Vercel handles this automatically)
PORT=5001

# ===== DEPLOYMENT CHECKLIST =====
# 
# Before deploying:
# [ ] MongoDB Atlas cluster is created and accessible
# [ ] Cloudinary account is set up
# [ ] JWT_SECRET is generated (32+ characters)
# [ ] All values above are filled in
# 
# After first deployment:
# [ ] Update ALLOWED_ORIGINS with your Vercel URL
# [ ] Run: node server/create_admin.js (locally or via Vercel CLI)
# [ ] Run: node server/config/setupIndexes.js
# [ ] Test the deployment
# [ ] Add a custom domain (optional)
